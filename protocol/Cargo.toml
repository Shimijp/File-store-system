[package]
name = "protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
# Rust's serialization framework
serde = { version = "1.0", features = ["derive"] }
# Binary encoding engine
bincode = "1.3"
