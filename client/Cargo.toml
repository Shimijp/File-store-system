[package]
name = "client"
version = "0.1.0"
edition = "2021"

[dependencies]
# Link to our local library
protocol = { path = "../protocol" }

tokio = { version = "1", features = ["full"] }
dotenvy = "0.15.7"
