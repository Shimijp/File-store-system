[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
# Link to our local library
protocol = { path = "../protocol" }

# Uncomment the line below if you decide to go with async later:
# tokio = { version = "1.35", features = ["full"] }
